#!/usr/bin/make -f

export PYBUILD_NAME = scs

%:
	dh $@ --buildsystem=pybuild

override_dh_auto_clean:
	rm -rf scs_source
	mkdir -p scs_source
	dh_auto_clean

override_dh_auto_configure:
	(rm -rf scs_source && git clone https://github.com/cvxgrp/scs.git scs_source)
